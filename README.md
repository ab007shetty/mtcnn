# Simple MTCNN Implementation.


## INSTALLATION

Currently it is only supported Python3.4 onwards. It can be installed through pip:

.. code:: bash

    $ pip install mtcnn

    $ pip install tensorflow

    $ conda install tensorflow


